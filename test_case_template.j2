import pytest

# 测试类
# 测试类
class TestAPI:

    @classmethod
    def setup_class(cls):
        """类级别的setup"""
        pass

    @classmethod
    def teardown_class(cls):
        """类级别的teardown"""
        pass

    def setup_method(self, method):
        """函数级别的setup"""
        pass

    def teardown_method(self, method):
        """函数级别的teardown"""
        pass

    {% for case in test_cases %}
    # 用例编号：{{ case.case_id }}
    def test_{{ case.test_name }}(self):
        """{{ case.description }}"""
        # 注入16进制字符串
        hex_string = "{{ case.hex_string }}"

        # 模拟调用接口（替换为实际接口调用代码）
        response = self.call_api(hex_string)

        # 校验结果（替换为实际校验逻辑）
        assert response == "{{ case.expected_result }}"
    {% endfor %}
